Drop your licensed font files here (`.otf`/`.ttf`). Not tracked in git — fonts
are usually licensed per-seat and shouldn't ship in a public repo.

`_quarto.yml` sets the body font by name (`Charter BT` by default). Point it
at whatever you put in this folder.
